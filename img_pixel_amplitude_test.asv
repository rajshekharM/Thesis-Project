rgb_img=imread('3.jpg');
imtest=rgb_img;
seg_img=segmentskin(imtest, hist_avg);
seg_img=im2double(seg_img);
rgb_seg_img=hsv2rgb(seg_img);


g_seg_img=rgb_seg_img(:,:,2);       
[nrows ncols] = size(g_seg_img);
amp=0;
count=0;
 for i = 1:nrows
   for j = 1:ncols
      if(g_seg_img(i,j) > 0) 
         count=count+1;
         
      end
   end
 end
 
 y=mean(amp(:));
 